%h1 Listing books

%table
  %thead
    %tr
      %th Name
      %th Author
      %th Description
      %th Status
      %th
      %th
      %th

  %tbody
    - @books.each do |book|
      %tr
        %td= link_to book.name, book_path(book.id)
        %td= book.author
        %td= book.description
        %td= book.status

%br

